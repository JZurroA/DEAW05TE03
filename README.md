# DEAW05TE03

Proyecto Spring Boot de Java + cliente JS integrado en el mismo proyecto (SPA).
Se montan tres contenedores para desplegar el proyecto: MySQL, phpMyAdmin y myapp (donde se aloja el fichero .jar del proyecto).
